@using Nest.API.Models
@model HashSet<Post>

<div>
    @if (Model.Count() > 0) {
        foreach (Post post in Model) {
            @Html.Partial("Post", post)
        }
    } else {
        <span>No chats found</span>
    }
</div>