@using Nest.Models
@model HashSet<Chat>

    <div>
       @if (Model.Count() > 0) {
            foreach (Chat chat in Model) {
                <span>Chat!</span>
            }
        } else {
            <span>No chats found</span>
        }
    </div>
